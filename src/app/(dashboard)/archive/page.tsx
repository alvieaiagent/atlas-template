import { Archive, Search } from "lucide-react";
import { UsageGuide } from "@/components/app/usage-guide";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { getPosts } from "@/lib/data";
import { getLanguage, pick } from "@/lib/language";
import { LEARNING_AREAS, WORTH_TAGS } from "@/lib/strategic-intelligence";

export default async function ArchivePage() {
  const language = await getLanguage();
  const copy = pick(language, {
    en: { eyebrow: "Archive", title: "Older crawls and historical summaries", intro: "Archive is a view/filter, not destructive deletion. Active Signals stay recent; Knowledge Bank saves stay forever; older unsaved crawls remain searchable here.", guideTitle: "How Archive works", guide: "Use Archive when the signal is older than 90 days, when you need historical context, or when you want to rescue an old item into Knowledge Bank. Do not delete unless the schema has safe hide/delete semantics.", tip: "Restore/hide/delete is intentionally omitted until the current schema safely supports those actions.", filters: "Archive filters", preview: "Archive preview", keyword: "Keyword search", empty: "No older-than-90-days crawled rows returned from the current data source. This is honest empty state, not sample archive data.", save: "Save to Knowledge Bank" },
    yue: { eyebrow: "Archive", title: "舊 crawls 同 historical summaries", intro: "Archive 係 view/filter，唔係 destructive deletion。Active Signals 保持近期；Knowledge Bank saves 永久保留；舊 unsaved crawls 喺呢度 search。", guideTitle: "Archive 點運作", guide: "Signal 超過 90 日、需要 historical context、或者想救返舊 item 入 Knowledge Bank，就用 Archive。除非 schema 有安全 hide/delete semantics，否則唔刪。", tip: "Restore/hide/delete 暫時刻意唔放，等 current schema 安全支持先加。", filters: "Archive filters", preview: "Archive preview", keyword: "Keyword search", empty: "Current data source 冇返回 older-than-90-days crawled rows。呢個係老實 empty state，唔係 sample archive data。", save: "儲入 Knowledge Bank" },
  });
  const oldPosts = await getPosts({ time: "all" });
  const archivePreview = oldPosts.filter((post) => { const fetched = new Date(post.fetchedAt).getTime(); return Number.isFinite(fetched) && fetched < Date.now() - 1000 * 60 * 60 * 24 * 90; }).slice(0, 12);

  return (
    <main className="flex min-w-0 flex-1 flex-col gap-5 p-4 md:p-6">
      <header><p className="text-sm font-bold text-blue-700">{copy.eyebrow}</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-slate-950">{copy.title}</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600"><strong className="text-slate-950">{copy.intro.split(".")[0]}.</strong>{copy.intro.slice(copy.intro.indexOf(".") + 1)}</p></header>
      <UsageGuide title={copy.guideTitle} description={copy.guide} steps={[{ label: "Active", title: "Last 90 days", body: "Recent fetched/scraped signals belong in Signals so Alvie can act quickly." }, { label: "Archive", title: "Older crawls", body: "Older crawled items and historical summaries remain accessible as filters." }, { label: "Knowledge", title: "Saved forever", body: "Saved Knowledge Bank items never expire from view, regardless of age." }]} tip={copy.tip} />
      <section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-4 flex items-center gap-2 text-blue-700"><Search className="h-4 w-4" /><h2 className="text-lg font-bold text-slate-950">{copy.filters}</h2></div><div className="grid gap-3 md:grid-cols-3 xl:grid-cols-4"><Input placeholder={copy.keyword} /><Input type="date" aria-label="Start date" /><Input type="date" aria-label="End date" /><select className="h-10 rounded-md border border-slate-300 bg-white px-3 text-sm font-medium text-slate-950"><option>All sources</option><option>YouTube</option><option>Instagram</option><option>X / Threads</option><option>Web / Blog</option></select><select className="h-10 rounded-md border border-slate-300 bg-white px-3 text-sm font-medium text-slate-950"><option>All learning areas</option>{LEARNING_AREAS.map((area) => <option key={area.slug}>{area.label}</option>)}</select><select className="h-10 rounded-md border border-slate-300 bg-white px-3 text-sm font-medium text-slate-950"><option>All worth tags</option>{WORTH_TAGS.map((tag) => <option key={tag}>{tag}</option>)}</select><select className="h-10 rounded-md border border-slate-300 bg-white px-3 text-sm font-medium text-slate-950"><option>All Janice verdicts</option><option>Useful</option><option>Needs fact-check</option><option>Noise</option></select><select className="h-10 rounded-md border border-slate-300 bg-white px-3 text-sm font-medium text-slate-950"><option>Saved / unsaved</option><option>Saved</option><option>Unsaved</option></select></div></section>
      <section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-4 flex items-center gap-2 text-blue-700"><Archive className="h-4 w-4" /><h2 className="text-lg font-bold text-slate-950">{copy.preview}</h2></div>{archivePreview.length ? <div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{archivePreview.map((post) => <article key={post.id} className="rounded-lg border border-slate-200 bg-slate-50 p-4"><p className="line-clamp-3 text-sm font-bold text-slate-950">{post.text || post.authorName}</p><p className="mt-2 text-xs text-slate-500">{post.source.toUpperCase()} · fetched {post.fetchedAt.slice(0, 10)}</p><Button className="mt-3" size="sm" variant="outline">{copy.save}</Button></article>)}</div> : <p className="rounded-lg border border-dashed border-slate-300 bg-slate-50 p-4 text-sm text-slate-500">{copy.empty}</p>}</section>
    </main>
  );
}
